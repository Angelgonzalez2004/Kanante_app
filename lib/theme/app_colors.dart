import 'package:flutter/material.dart';

class AppColors {
  static const Color background = Color(0xFFF8F9FA); // Almost white
  static const Color primary = Color(0xFFADC8D8);    // Soft blue
  static const Color accent = Color(0xFFBDE0FE);     // Lighter, airy blue
  static const Color textDark = Color(0xFF343A40);   // Dark gray for text
  static const Color textLight = Color(0xFF6C757D);  // Lighter gray for subtitles
  static const Color white = Colors.white;
  static const Color error = Color(0xFFE63946);
  static const Color success = Color(0xFF2A9D8F);
}
